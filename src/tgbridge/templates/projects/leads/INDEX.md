# Leads — Pipeline

Leads são pastas flat (`L<NN>-<slug>/README.md`). O **estágio** vive no frontmatter de cada
lead (`stage:`); esta visão agrupa por estágio e é **regenerável**
(`scripts/regen-indexes.sh`) — não mova pastas para reorganizar, só mude o `stage:`.
Mantenha os marcadores.

<!-- regen:leads:start -->
| Estágio | Itens | Próxima ação |
|---------|-------|--------------|
| _(vazio — crie o primeiro lead a partir de `../_PROJECT_README.md` com `type: lead`)_ | | |
<!-- regen:leads:end -->

**Voltar:** [[../INDEX]]
